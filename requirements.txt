# hosted_auth_service dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.115.14
uvicorn[standard]==0.34.3

# Settings & validation
pydantic==2.11.7
pydantic-settings==2.10.1

# JWT (PyJWT with cryptographic extras for RS256 + EdDSA)
PyJWT[crypto]==2.10.1

# HTTP client (async, for JWKS fetch)
aiohttp==3.12.13

# Database (PostgreSQL async)
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0
psycopg[binary]==3.2.9
alembic==1.16.2

# NATS (token storage & cache)
nats-py[nkeys]==2.10.0

# Testing
pytest==8.4.1
pytest-asyncio==1.0.0
httpx==0.28.1
